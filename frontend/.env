# URL base del backend Django (incluye el prefijo /api)
VITE_API_URL=http://localhost:8000/api

# "false" conecta autenticación y el flujo de citas del paciente al backend
# Django. Déjalo en "true" solo para demostraciones aisladas sin API.
VITE_USE_MOCK=false
